# Core API framework
fastapi==0.115.12
uvicorn==0.34.3
pydantic==2.11.7
python-dotenv==1.1.1

# HTTP
requests==2.32.4

# ML / risk scoring
scikit-learn==1.6.1
shap==0.47.2
numpy==2.2.6
pandas==2.2.4

# Encryption (Issue #4: pinned exact versions confirmed from pip 2026-08)
cryptography==50.0.0
argon2-cffi==25.1.0

# MFA (Issue #6: TOTP with replay protection)
pyotp==2.10.0
qrcode==8.2

# Vault client (optional — only needed when VAULT_ADDR is configured)
hvac==2.4.0

# OIDC Login
google-auth==2.56.3
httpx==0.28.1
pillow==12.3.0
python-multipart>=0.0.18
